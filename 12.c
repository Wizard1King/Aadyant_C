// C Program to Read Integer and Print First Three Powers (N^1, N^2, N^3)
#include<stdio.h>
#include<math.h>
int main(){
    double n;
    printf("enter a number : ");
    scanf("%lf", &n);

    printf("the first three powers of the number are %lf\n %lf\n %lf\n", pow(n,1), pow(n,2), pow(n,3));














    return 0;
}